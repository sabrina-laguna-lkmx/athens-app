// import { generalMetadata } from "@/app/shared-metadata";
import Settings from "@/components/settings";

// export const metadata: Metadata = {
//   ...generalMetadata("Settings", "noindex, nofollow", "/app/settings"),
// };

export default function Page() {
  return <Settings />;
}

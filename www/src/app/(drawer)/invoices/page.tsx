import Invoices from "@/components/invoices";
// import { generalMetadata } from "@/app/shared-metadata";

// export const metadata: Metadata = {
//   ...generalMetadata("Invoices", "noindex, nofollow", "/invoices"),
// };

export default function Page() {
  return <Invoices />;
}

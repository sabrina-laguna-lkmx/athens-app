'use strict';

/**
 * earth-commitment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::earth-commitment.earth-commitment');
